# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @galleria/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t galleria-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system galleria-auth-service && \
          adduser --system -G galleria-auth-service galleria-auth-service

COPY apps/auth-service/dist galleria-auth-service/
COPY apps/auth-service/package.json galleria-auth-service/
RUN chown -R galleria-auth-service:galleria-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix galleria-auth-service --omit=dev -f install

CMD [ "node", "galleria-auth-service" ]
